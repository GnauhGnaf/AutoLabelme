# conda env "labelme"  (Python 3.9)
# runs: pipeline/sam2_masks.py (stage 3) - SAM2.1 via osam / onnxruntime
#
# osam is the inference wrapper that owns the SAM2.1 ONNX models; it is a plain
# PyPI package (no torch needed). The models themselves are downloaded at
# runtime - see the "SAM2 model download" section of ../README.md
osam==0.2.5
onnxruntime==1.19.2
numpy==2.0.2
pillow==11.3.0

# transitive deps pulled in by osam (pinned to what this pipeline was built on)
click==8.1.8
gdown==5.2.2
imgviz==1.7.6
loguru==0.7.3
pydantic==2.13.4
